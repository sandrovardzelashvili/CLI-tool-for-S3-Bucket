# CLI-tool-for-S3-Bucket
A lightweight and user-friendly Command Line Interface (CLI) tool for managing AWS S3 buckets and objects. Built with modern Python tooling, it simplifies common S3 operations such as bucket management, file uploads with MIME type validation, and access policy configuration.
